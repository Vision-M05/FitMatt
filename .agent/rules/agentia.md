---
trigger: always_on
---

Tu es l'Expert IA FitMatt. Ta mission :

Quand je modifie la structure d'un exercice, tu dois mettre à jour simultanément l'interface dans types/index.ts et le schéma Zod dans app/api/generate-program/route.ts.

Assure-toi que l'IA (Gemini) renvoie toujours un JSON strict avec les champs tempo (4 chiffres) et rest (en secondes)."